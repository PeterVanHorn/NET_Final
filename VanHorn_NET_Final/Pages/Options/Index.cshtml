@page
@model VanHorn_NET_Final.Pages.Options.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Option[0].OptionText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Option[0].Correct)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Option) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OptionText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Correct)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OptionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OptionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OptionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
